
    {% for child_link in linklists.custom-women.links %}
        {% comment %} {% assign image_address = 'megamenu_mobile_men_image_' | append: forloop.index %}
        {% assign the_image = settings[image_address] %} {% endcomment %}
        {% if forloop.index > 2 and forloop.index < 12 %}
          <li class="mobile-nav__item">
            <div class="mobile-nav__item__inner mobile-nav__item__inner--submenu">
                {% comment %} {%- assign img_url = the_image | img_url: '748x748', crop: 'center' | replace: '_1x1', '_{width}x{height}' -%}
                {%- assign widths = '[180, 200, 360, 540, 720, 900, 1080, 1296, 1512, 1728, 2048]' -%} {% endcomment %}
                <a href="{{ child_link.url }}"
                  class="mobile-nav-link mobile-nav-link--submenu">
                    {{ child_link.title }}
                </a>
            </div>
          </li>
        {% endif %}
    {% endfor %}

<li class="mobile-nav__item has-submenu">
  <div class="mobile-nav__item__inner">
    <a class="mobile-nav-link" >
      shop by shape
    </a>
    <button class="btn btn--plain mobile-nav-more" data-menu-more-toggle>
      <span class="material-icons custom-icon-expand_more"></span>
    </button>
  </div>
  <ul class="mobile-nav mobile-nav__submenu ff--heading" data-menu-submenu   data-mobile-shop-submenu>
    {% for child_link in linklists.custom-women.links %}
        {% comment %} {% assign image_address = 'megamenu_mobile_men_image_' | append: forloop.index %}
        {% assign the_image = settings[image_address] %} {% endcomment %}
        {% if forloop.index > 11 and forloop.index < 16 %}
          <li class="mobile-nav__item">
            <div class="mobile-nav__item__inner mobile-nav__item__inner--submenu">
                {% comment %} {%- assign img_url = the_image | img_url: '748x748', crop: 'center' | replace: '_1x1', '_{width}x{height}' -%}
                {%- assign widths = '[180, 200, 360, 540, 720, 900, 1080, 1296, 1512, 1728, 2048]' -%} {% endcomment %}
                <a href="{{ child_link.url }}"
                  class="mobile-nav-link mobile-nav-link--submenu">
                    {{ child_link.title }}
                </a>
            </div>
          </li>
        {% endif %}
    {% endfor %}
  </ul>
</li>
<li class="mobile-nav__item has-submenu">
  <div class="mobile-nav__item__inner">
    <a class="mobile-nav-link" >
      shop by trend
    </a>
    <button class="btn btn--plain mobile-nav-more" data-menu-more-toggle>
      <span class="material-icons custom-icon-expand_more"></span>
    </button>
  </div>
  <ul class="mobile-nav mobile-nav__submenu ff--heading" data-menu-submenu   data-mobile-shop-submenu>
    {% for child_link in linklists.custom-women.links %}
        {% comment %} {% assign image_address = 'megamenu_mobile_men_image_' | append: forloop.index %}
        {% assign the_image = settings[image_address] %} {% endcomment %}
        {% if forloop.index > 15 %}
          <li class="mobile-nav__item">
            <div class="mobile-nav__item__inner mobile-nav__item__inner--submenu">
                {% comment %} {%- assign img_url = the_image | img_url: '748x748', crop: 'center' | replace: '_1x1', '_{width}x{height}' -%}
                {%- assign widths = '[180, 200, 360, 540, 720, 900, 1080, 1296, 1512, 1728, 2048]' -%} {% endcomment %}
                <a href="{{ child_link.url }}"
                  class="mobile-nav-link mobile-nav-link--submenu">
                    {{ child_link.title }}
                </a>
            </div>
          </li>
        {% endif %}
    {% endfor %}
  </ul>
</li>

{% for child_link in linklists.custom-women.links %}
  {% if forloop.index < 3 %}
    {% assign image_address = 'megamenu_image_' | append: forloop.index %}
    {% assign the_image = settings[image_address] %}
    {% if the_image %}
      <li>
        <div class="mobile-shop-submenu__image-wrappers">
            {%- assign img_url = the_image | img_url: '748x748', crop: 'center' | replace: '_1x1', '_{width}x{height}' -%}
            {%- assign widths = '[180, 200, 360, 540, 720, 900, 1080, 1296, 1512, 1728, 2048]' -%}
            <a href="{{ child_link.url }}"
              class="mobile-nav-link mobile-nav-link--submenu mobile-nav-link--submenu--shop">
                <img
                        src="data:image/gif;base64,R0lGODlhAQABAAAAACH5BAEKAAEALAAAAAABAAEAAAICTAEAOw=="
                        data-src="{{ img_url }}"
                        data-sizes="auto"
                        data-widths="{{ widths }}"
                        class="responsive-image__imgs mobile-shop-submenu__images"
                        sizes="(max-width: 768px) 200px"
                        width="200"
                        height="200"
                >
              <span>{{ child_link.title }}</span>
            </a>
        </div>
      </li>
    {% endif %}
  {% endif %}
  
{% endfor %}